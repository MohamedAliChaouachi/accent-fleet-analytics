# Accent Fleet Analytics — CRISP-DM Phase 3 refactored pipeline
# Python 3.11+

# Core data + DB
sqlalchemy>=2.0.30
psycopg[binary]>=3.1.18
polars>=1.0.0
pyarrow>=16.0.0
pandas>=2.2.0         # kept for notebook interop; polars is the engine

# Config & contracts
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1

# Orchestration (batch + streaming)
prefect>=3.0.0

# Streaming stubs (kafka consumer — activate when infra is ready)
confluent-kafka>=2.4.0

# Notebooks
jupyterlab>=4.2.0
ipykernel>=6.29.0
matplotlib>=3.9.0
seaborn>=0.13.0
plotly>=5.22.0
scikit-learn>=1.5.0
imbalanced-learn>=0.14.0

# Quality & testing
pytest>=8.2.0
pytest-cov>=5.0.0
great-expectations>=0.18.0    # optional, used in monitoring module

# Dev / ergonomics
python-dotenv>=1.0.0
rich>=13.7.0                  # pretty CLI logs
structlog>=24.1.0             # structured logging
prometheus-client>=0.20.0     # /metrics endpoint + ETL counters

# --- v0.5 operationalization (API + dashboard + MLflow) ---
# API service
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0                 # API client used by tests and dashboard What-If

# ML lifecycle
# Pin to the 2.x line: server image ghcr.io/mlflow/mlflow:v2.16.2 doesn't
# expose the /api/2.0/mlflow/logged-models endpoint that mlflow 3.x clients call,
# so a 3.x client against this server fails with 404 during log_model().
mlflow>=2.16.0,<3.0.0
joblib>=1.4.0

# --- v0.9.0 auth + multi-tenancy ---
# Argon2id is the PHC password-hashing winner and OWASP-recommended primitive
# (see docs/auth_design.md §1.2 threat #6). argon2-cffi ships the canonical
# Python binding around the reference C library.
argon2-cffi>=23.1.0
# PyJWT is single-responsibility (issue + verify), well-audited, and avoids
# the heavier python-jose surface area. HS256 is the only algorithm we use
# at v0.9.0; RS256 swap is documented for §2.4.
PyJWT>=2.9.0
# pydantic.EmailStr in our auth schemas (login email, admin create-user)
# requires this validator. Without it pydantic raises ImportError at class
# definition time, which would crash app startup.
email-validator>=2.2.0

# --- Text2SQL AI assistant (POST /v1/ai/query) ---
# AST-based SQL validator — sql_guard parses LLM output under the
# postgres dialect and walks the tree to reject anything that isn't a
# read-only SELECT, with tenant predicate enforcement.
sqlglot>=25.0.0
# LLM provider SDKs. Both are listed; the runtime picks one via the
# AI_PROVIDER env var. Marked as optional in spirit — if you only need
# one provider, comment the other out (the factory lazy-imports so a
# missing SDK doesn't break app boot, only /v1/ai/query for that vendor).
openai>=1.40.0
anthropic>=0.34.0
redis[hiredis]>=5.0.0
